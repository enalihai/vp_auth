<center>
  <h1> Viewing Party Lite </h1>
 <% flash.each do |type, msg| %>
      <div> <%= msg %> </div>
    <% end %>
  <div>
    Existing Users: <br>
      <% @users.each do |user| %>
        <%= link_to "#{user.email}'s Dashboard", "/users/#{user.id}" %> </br>
      <% end %>
  </div>
  
</center>
